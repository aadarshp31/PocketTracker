export function DashboardPage() {
  return (
    <section>
      <h1>Dashboard</h1>
      <p className="muted">Insights-focused view is pending backend summary endpoints.</p>
      <ul className="status-list">
        <li>✅ Transaction CRUD exists in backend</li>
        <li>✅ Pagination metadata exists in backend</li>
        <li>⏳ Monthly summary endpoint pending</li>
        <li>⏳ Category breakdown endpoint pending</li>
      </ul>
    </section>
  )
}
